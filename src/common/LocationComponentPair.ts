export class LocationComponentPair
{
    location: number;
    component: number;

    constructor(location: number, component: number)
    {
        this.location = location;
        this.component = component;
    }
}